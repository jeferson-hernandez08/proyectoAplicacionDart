void main() {
  //Tenemos una lista de nombres y queremos imprimir cada nombre usando forEach.

  List<String> nombres = ['Ana', 'Juan', 'María', 'Pedro'];

  // Usando forEach para imprimir cada nombre
  nombres.forEach((nombre) {
    print(nombre);
  });
}